name: Build Huda School APK (Flet)
on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Set up Java (JDK 17)
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # Pre-installing Flutter here (instead of letting `flet build` download
      # it on the fly) makes the build faster and its progress visible in the
      # Actions log instead of appearing to hang.
- name: Set up Flutter
  uses: subosito/flutter-action@v2
  with:
    flutter-version: '3.44.8'
    channel: 'stable'
      - name: Install Flet
        run: python -m pip install --upgrade pip && python -m pip install flet==0.86.5

      - name: Build APK
        run: flet build apk -v

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: huda-school-apk
          path: build/apk/*.apk
