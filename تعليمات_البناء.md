# نظام إدارة مدرسة الهدى الثانوية بنين - نسخة Flet (أوفلاين بالكامل)

## لماذا Flet
- التطبيق Flutter أصلي بالكامل — لا يوجد سيرفر Flask ولا WebView.
- دعم أصلي لاتجاه الكتابة من اليمين لليسار (`page.rtl = True`)، فالعربية
  تُعرض وتُحاذى بشكل صحيح من غير أي حيل يدوية لتشكيل الحروف.
- عملية البناء (`flet build apk`) تعتمد مباشرة على أدوات Flutter الرسمية
  لبناء أندرويد، فهي أنضج وأقل عرضة لمشاكل توافق Gradle التي واجهتها
  سابقًا مع Buildozer.
- تُثبَّت أدوات Flutter و Java و Android SDK تلقائيًا عند أول تشغيل لأمر
  `flet build` إذا لم تكن موجودة (نسخة GitHub Actions المرفقة تُثبّت
  Flutter مسبقًا لتسريع البناء وإظهار تقدمه بوضوح في السجل).

## الملفات
- `main.py` — كل كود التطبيق في ملف واحد: الطلاب، المعلمون، الفصول،
  المواد، الحضور والغياب، الدرجات، الرسوم الدراسية.
- `pyproject.toml` — إعدادات حزمة أندرويد (اسم الحزمة، الإصدار الأدنى...).
- `requirements.txt` — يحدد إصدار مكتبة Flet.
- `.github/workflows/build.yml` — يبني ملف APK تلقائيًا عبر GitHub Actions.
- `assets/fonts/` — ضع هنا ملف **Amiri-Regular.ttf** (نفس الخط الذي
  استخدمته سابقًا). بدونه سيعمل التطبيق لكن بالخط الافتراضي.

## اختبار تم إجراؤه فعليًا قبل التسليم
تم تشغيل منطق التطبيق بالكامل آليًا (إضافة/حذف طلاب، معلمين، فصول، مواد،
حضور، درجات، رسوم) بما في ذلك حالة حذف فصل مرتبط بطالب - يعرض الآن رسالة
تنبيه بدل تعطل التطبيق. هذا لا يغني عن تجربتك الفعلية للتطبيق على الهاتف،
لكنه يستبعد أخطاء برمجية شائعة قبل وصوله إليك.

## خطوات البناء
1. أنشئ مستودع جديد على GitHub وارفع كل هذه الملفات، بما فيها
   `Amiri-Regular.ttf` داخل `assets/fonts/`.
2. من تبويب Actions، شغّل الـ workflow يدويًا أو ادفع (push) إلى main.
3. بعد اكتمال البناء، حمّل ملف APK من قسم Artifacts باسم `huda-school-apk`.
4. انقله لهاتفك وثبّته (فعّل "السماح بالتثبيت من مصادر غير معروفة" إذا طُلب).

## بعد التثبيت
- التطبيق يعمل بدون إنترنت من أول تشغيل.
- البيانات محفوظة في تخزين خاص بالتطبيق على الهاتف (لا تُحذف إلا بحذف
  التطبيق أو مسح بياناته يدويًا من إعدادات أندرويد).

## ملاحظة
لم تُضَف بعد ميزتا تصدير PDF واستيراد/تصدير Excel. الأفضل التأكد أولًا أن
هذه النسخة الأساسية تُبنى وتعمل بنجاح فعليًا على جهازك، ثم نضيفهما بعد ذلك.
